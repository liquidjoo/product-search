
#spring.cloud.config.label=local

#spring.cloud.config.discovery.enabled=true

spring:
  application:
    name: application
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888

#  config:
#    activate:
#      on-profile: dev
management:
  endpoints:
    web:
      exposure:
        include: ['refresh']

api:
  test: 123
