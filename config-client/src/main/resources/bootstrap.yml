
#spring.cloud.config.label=local

#spring.cloud.config.discovery.enabled=true

spring:
  application:
    name: token
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888

#  config:
#    activate:
#      on-profile: dev
management:
  endpoints:
    web:
      exposure:
        include: ['refresh']
